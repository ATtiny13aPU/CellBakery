#version 430

layout(local_size_x = 1, local_size_y = 1) in;

uniform int Dm;

struct Chunk {
	int linked_list;
	float brightness;
};

restrict buffer ssbo_grid {
    Chunk chunks[];
};

void main() {

	chunks[gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * Dm].linked_list = 0;
	chunks[gl_GlobalInvocationID.x + gl_GlobalInvocationID.y * Dm].brightness = 0.;
}